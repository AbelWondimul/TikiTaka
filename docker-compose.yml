version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: always

  # Future: Add Firebase Emulators here if needed
